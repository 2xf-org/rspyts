// Code generated by rspyts v0.2.0. DO NOT EDIT.
// rspyts:manifest-hash sha256:bcb4295a5648b6010d7a1b2f4328fa31fcdd4b96af7993bce8a914cb74a4d3d2

import { type BridgeErrorRegistry, RspytsError, floatFromWire, u64FromWire } from "rspyts";

/** Failure modes of the basic functions. */
export class BasicError extends RspytsError {}

/** The input contained no values. */
export class BasicErrorEmptyInput extends BasicError {
  constructor(message: string, data?: unknown) {
    super(message, "emptyInput", data);
  }
}

/**
 * The input contained more than [`MAX_WINDOW`] values.
 *
 * `.data`: { count: number }
 */
export class BasicErrorTooManyValues extends BasicError {
  constructor(message: string, data?: unknown) {
    super(message, "tooManyValues", data);
  }
}

/**
 * The text could not be parsed as a number.
 *
 * `.data`: { text: string }
 */
export class BasicErrorNotANumber extends BasicError {
  constructor(message: string, data?: unknown) {
    super(message, "notANumber", data);
  }
}

/**
 * The scale factor must be non-zero.
 *
 * `.data`: { factor: number }
 */
export class BasicErrorZeroFactor extends BasicError {
  constructor(message: string, data?: unknown) {
    super(message, "zeroFactor", data === undefined ? undefined : ((value: { factor: number }) => ({ ...value, factor: floatFromWire(value.factor) }))(data as { factor: number }));
  }
}

/**
 * The file could not be read.
 *
 * `.data`: { path: string }
 */
export class BasicErrorUnreadableFile extends BasicError {
  constructor(message: string, data?: unknown) {
    super(message, "unreadableFile", data);
  }
}

/**
 * An exact sequence exceeded the caller's exact limit.
 *
 * `.data`: { value: bigint; maximum: bigint }
 */
export class BasicErrorSequenceTooLarge extends BasicError {
  constructor(message: string, data?: unknown) {
    super(message, "sequenceTooLarge", data === undefined ? undefined : ((value: { value: string; maximum: string }) => ({ ...value, value: u64FromWire(value.value), maximum: u64FromWire(value.maximum) }))(data as { value: string; maximum: string }));
  }
}

export const BasicErrorTypes = {
  "emptyInput": BasicErrorEmptyInput,
  "tooManyValues": BasicErrorTooManyValues,
  "notANumber": BasicErrorNotANumber,
  "zeroFactor": BasicErrorZeroFactor,
  "unreadableFile": BasicErrorUnreadableFile,
  "sequenceTooLarge": BasicErrorSequenceTooLarge,
} as const satisfies BridgeErrorRegistry;
